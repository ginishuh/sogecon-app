# 2025-12-16

- Fixed: CI `web` 번들 사이즈 체크가 admin 전용 청크(`app/admin/**`)까지 합산해 실패하던 문제를 개선(제외 옵션 추가).
- Security: `pnpm audit --audit-level=high` 실패를 해결하기 위해 Next.js 15.5.9로 업데이트.
- UX: 게시물/행사 관리 목록에서 배너(히어로) 지정/해제 및 PIN 토글을 바로 할 수 있도록 개선(lookup 엔드포인트 추가).
- UX: 게시글/행사 생성 동선을 `/admin/.../new`로 통합하고 기존 `/posts/new`, `/events/new`는 관리자 생성 화면으로 리다이렉트.
- Admin: `/admin/hero`는 고급 설정/삭제 화면으로 안내 문구를 추가해, “생성” 중심 동선을 줄임.
- Dev: `make api-start`가 패키지 상대 import로 실패하던 문제를 수정(uvicorn 엔트리/경로 정정).
- Dev: Web Vitals 전송 URL을 API_BASE 기준으로 통일해 `/rum/vitals` 404 노이즈 제거.
- UX: 상단/드로어 관리자 메뉴에서 “새 글/새 행사” 바로가기는 제거하고(중복), 관리 목록 내 생성 버튼만 유지.
- UX: 관리자 게시물/행사 관리에서 “홈 배너” 컬럼 표기/안내 추가, 액션 UI는 텍스트로 통일하고 행사 “보기” 버튼은 제거.
- UX: 홈 배너 토글이 즉시 반응하도록 optimistic 업데이트 적용 + 게시글/행사 수정 화면에도 홈 배너 토글 섹션을 추가.
- UX: 전역 링크 스타일을 B안으로 정리(본문 링크는 `.text-link`, 버튼/CTA는 `ButtonLink`로 분리)해 언더바/visited 혼선을 제거.
- UX: 홈 화면 데스크톱에서만 공지/행사/소식 섹션을 카드/리스트 형태로 추가하고, 모바일은 기존 레이아웃을 유지.
- UX: 홈 히어로 배너 이미지 `sizes/quality`를 조정해 데스크톱에서 흐릿하게 보이던 현상을 완화.
- Fix: Next typedRoutes 빌드에서 데스크톱 홈 섹션 링크 타입 오류를 수정.
- Push/PWA: iOS 홈 화면 아이콘 대응을 위해 PNG/마스크블 아이콘 + `apple-touch-icon` 추가, manifest/head 갱신.
- Push: dev 기본 SW 비활성 정책을 `ensureServiceWorker()`/푸시 CTA까지 일원화(`NEXT_PUBLIC_ENABLE_SW=1`), 의도치 않은 SW 등록 방지.
- API: 구독 저장 시 `member_id` 기록 + `PUSH_ENCRYPT_AT_REST=true`에서 `p256dh/auth`까지 at-rest 암호화(테스트 보강).
- UX: 홈 “동문회장 인사말” 카드를 이미지형으로 전환(데스크톱/모바일 분리 + 여백 보정).
