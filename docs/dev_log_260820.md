# 2026-08-20

- Changed: `private`는 불특정 동문에게 비공개이고, 개별 열람 허용은 같은 화면에서 취소한다.
- Added: 열람 요청 `revoked` 상태와 `POST /me/view-requests/{id}/revoke`.
- Fixed: 기존 `private` 회원의 `directory_consent_at`을 비우고, 거절·철회 후 재요청 시각을 갱신한다.
