# 2026-07-28 개발 로그

## Web 의존성 보안 패치

- Ready PR의 Web 의존성 감사에서 새로 탐지된 Next.js, Sharp, PostCSS 고위험 취약점을 패치했다.
- Next.js와 번들 분석기를 `16.2.11`, PostCSS를 `8.5.18`로 올리고, Next.js의 선택 의존성이 취약한 Sharp `0.34.x`를 다시 선택하지 않도록 workspace override를 `0.35.3`으로 고정했다.
- 버전 잠금 검사와 `pnpm-lock.yaml`을 함께 갱신했다.
- 저장소 가드, 버전 잠금, Web 보안 감사, lint, 단위 테스트 239건, Next.js production build를 통과했다.
