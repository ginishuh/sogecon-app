# 2026-02-24

- Fixed: 안드로이드 PWA 아이콘이 구버전/대체 글자 아이콘으로 남는 문제 수정 (frontend impact)
- Changed: `public/sw.js`에서 `/icons/*`, `/favicon.ico`, `/favicon.svg`를 이미지 Cache First 대상에서 제외하고 SW 캐시 버전 `v3`로 상향
- Changed: `public/manifest.json` 아이콘 경로에 버전 쿼리(`?v=20260224`)를 부여해 아이콘 갱신 강제
- Changed: `manifest` 아이콘 목록에서 SVG 항목을 제거하고 PNG(+maskable)만 유지해 Android 런처 호환성 안정화
- Chore: 더 이상 사용하지 않는 `public/icons/icon-192.svg`, `public/icons/icon-512.svg` 정리
- Fixed: `app/favicon.ico/route.ts` 제거로 `public/favicon.ico`가 정상 서빙되도록 정리
- Ops/Docs: `docs/pwa_push.md`에 아이콘 교체 시 `manifest` 버전 쿼리(`?v=YYYYMMDD`) 동시 갱신 규칙 명시
