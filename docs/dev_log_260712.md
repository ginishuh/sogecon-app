# 2026-07-12

- Changed: openapi-typescript 7.13.0, sharp 0.35.3, png-to-ico 3.0.2로 업데이트 (#180)
- Changed: pnpm override 6개를 재검증해 Vite·js-yaml·PostCSS 보안 핀만 유지하고 나머지 3개 제거
- Fixed: 재생성된 maskable 아이콘의 PWA 캐시 버스터 갱신 (frontend impact)
- Ops/Docs: workspace·schemas 도구 버전 SSOT와 CI 버전 가드 동기화
- Notes: OpenAPI/DTO semantic diff, 아이콘 생성, Web 품질·빌드·audit로 호환성 검증
- Changed: Next.js·bundle analyzer 16.2.10, `proxy.ts`, image·typed routes·standalone 설정으로 마이그레이션 (#181, frontend impact)
- Fixed: Turbopack bundle 측정과 RSC 자동 E2E를 안정화하고 ESLint 9 유지/#186 후속 경계를 문서화
- Fixed: #181 그록 리뷰 반영 — Next 16 client manifest 구조 계측과 fail-closed 번들 게이트, CSP 경로·E2E assertion 보강 (frontend impact)
- Fixed: #181 그록 재리뷰 Low 정리 — proxy 용어, 배너 전환 hard assertion, 물리 청크 누락 회귀 테스트 보강 (frontend impact)
- Changed: 루트 `AGENTS.md`를 저장소 실행 SSOT와 작업별 문서 라우터로 간소화
- Changed: Claude·Gemini·Copilot 지침을 얇은 어댑터로 전환하고 영문 복제 베이스·동기화 스크립트 제거
- Ops/Docs: repo guard에 하네스 구조 검증을 추가하고 README·CI·VPS 런북의 SSOT 안내 현행화
- Changed: #182 Tailwind CSS 3.4.19 → 4.3.2 및 전용 PostCSS 4.3.2로 마이그레이션 (frontend impact)
- Changed: 공식 upgrade tool 결과를 CSS-first 토큰·utility 구조로 수동 검토하고 autoprefixer 제거
- Notes: production CSS 50,338 → 66,483 bytes(+16,145, +32.1%); globals/theme/component utility를 174/139/390줄로 분리
- Fixed: #182 production E2E에서 Next Link 목적지 계약과 directory 화면 검증을 분리해 RSC 클릭 flake 제거
- Fixed: #182 홈 E2E의 `/hero/`를 2장 fixture로 고정해 로컬 DB 상태에 따른 배너 수 변동 제거
- Fixed: #182 그록 리뷰 반영 — 전역 base cascade 복원, Figma dead selector 제거, Preflight 호환 보강
- Fixed: #182 Windows 시각 검증 반영 — Figma 프리뷰 헤더·히어로·빠른 실행 selector hook 복구 (frontend impact)
- Changed: #183 Vitest 3.2.4 → 4.1.10, Vite 6.4.3 → 8.1.4, jsdom 25.0.1 → 29.1.1로 마이그레이션
- Fixed: Vitest 3용 Vite 보안 override를 제거하고 workspace 직접 핀으로 의존성 해석을 단일화
- Notes: Web 30파일 93테스트와 CDP E2E 3파일 3흐름에서 silent skip 없이 호환성 확인
- Changed: #184 bcrypt 4.2.0 → 5.0.0으로 업데이트하고 해시·검증·seed 정책을 공통화
- Fixed: 신규 비밀번호는 UTF-8 72바이트를 초과하면 거부하고 기존 bcrypt 4 해시는 절삭 검증 호환 유지 (frontend impact)
- Tests: ASCII·Unicode 72/73바이트 경계, 운영 형식 hash fixture, 로그인·활성화·비밀번호 변경 검증 추가
- Fixed: #184 Windows E2E 반영 — Pydantic 422 detail 배열을 한국어 문자열로 정규화하고 clean production 재기동 절차 검증 (frontend impact)
- Fixed: #184 재검증 반영 — 비밀번호 변경의 72바이트 초과 거부를 인라인 상태 메시지로 안내 (frontend impact)
- Changed: #185 web-vitals 4.2.3 → 5.3.0 및 RUM rating/navigationType 계약 마이그레이션
- Fixed: RUM fetch 실패를 사용자·Sentry 노이즈 없이 종료하고 개인정보 없는 payload 계약을 검증 (frontend impact)
- Fixed: #185 리뷰 반영 — v4 `back_forward`를 v5 `back-forward`로 정규화해 롤링 배포의 RUM 422 유실 방지
- Ops: #187 npm/pip/GitHub Actions 월간 Dependabot과 advisory 기반 security update 정책 구축
- Docs: major 독립 PR·자동 병합 금지·취약점 SLA·waiver 필수 메타데이터를 문서화하고 설정 검증 CI 추가
- Fixed: #187 리뷰 반영 — directory·group 구조·minor/patch allowlist를 CI에서 fail-closed 검증
- Fixed: Dependabot 표준 `build(deps)`·`build(deps-dev)` 커밋을 정식 scope로 허용하고 허용·거부 훅 회귀 테스트 추가
- Changed: #210 로그인·가입·활성화·문의·알림 문구를 동문 사용자 언어로 통일 (frontend impact)
- Fixed: 내부 VAPID·token·role·RBAC·오류 객체 노출을 일반 안내와 다음 행동으로 대체
- Docs/Tests: 동문 용어표 SSOT와 공개 문구 정적 검색·오류 매핑 회귀 테스트 추가
- Fixed: #210 그록 리뷰 반영 — 알림·첫 로그인·FAQ 문구 통일과 공개/운영 오류 fallback 정책 분리
- Fixed: #210 재리뷰 반영 — 내 정보 raw 오류 차단, 사무국 승인 문구와 공통 진입점 상수 통일
- Changed: #211 가입 신청→사무국 확인→비밀번호 만들기→첫 로그인 완료 4단계 여정 통합 (frontend impact)
- Fixed: 승인 전 불가능한 버튼 제거, 안내 링크 우선·코드 입력 fallback·만료/사용 완료 복구 행동 추가
- Tests/Docs: 가입 상태·활성화 링크·로그인 경로 회귀 테스트와 사용자 언어 SSOT 동기화
- Fixed: #211 그록 리뷰 반영 — 링크 검증 전 상태 단정 제거, URL 코드 즉시 정리, 오류 ARIA/focus와 E2E 계약 보강
- Fixed: #211 재리뷰 반영 — 로그인 오류 필드 연결, 가입 오류 alert, 실제 `/activate` URL 제거 회귀 검증 강화
- Changed: #212 홈을 역할별 핵심 행동과 공지·행사·동문 소식 중심의 단일 반응형 정보 계층으로 재구성 (frontend impact)
- Fixed: #212 그록 리뷰 반영 — 인증 확인 중 비회원 행동 노출을 막고 역할별 렌더·제목 계층·44px 포커스 영역·뉴스 반응형 계약 보강 (frontend impact)
- Changed: #213 자유게시판·질문·동문 이야기/행사 후기·경조사·공지·동문 소식을 목적별 사용자 언어와 정보 계층으로 재구성 (frontend impact)
- Fixed: raw category·`회원123`·개발자용 beta 문구를 제거하고 비회원 작성 경계·검색 무결과·오류 재시도·댓글 label을 보강 (frontend impact)
- Fixed: #213 그록 리뷰 반영 — unknown category 위장 방지, 댓글 인증 상태 분리, 첫 글 CTA·44px 조작·행사 후기 썸네일 보강 (frontend impact)
- Fixed: #213 재리뷰 반영 — 글쓰기 인증 오류 복구, 댓글 재시도·삭제 터치 영역, 게시 유형·중요 공지 접근성 명칭 정리 (frontend impact)
- Fixed: #213 Windows E2E 반영 — `/board/new`만 비회원 복구 화면 진입을 허용하고 목록·상세·수정 인증 경계 유지 (frontend impact)
- Fixed: #213 Windows E2E 반영 — `/media/**` 상대 이미지를 API origin으로 정규화해 상세·후기 목록 optimizer 깨짐 방지 (frontend impact)
# #214 동문회 디자인 시스템과 반응형 접근성 기준 정비

- Tailwind theme의 단일 소유권과 `globals.css`·component utility 책임을 문서화했다.
- Button과 ButtonLink의 변형·크기·focus 계약을 공통 SSOT로 합치고 모든 크기에 최소 44×44px 목표를 적용했다.
- Input·Select·TextArea의 label, helper, error와 공통 외형을 단일 구현으로 합쳤으며 오류를 `role="alert"`로 연결했다.
- Tabs의 모바일 overflow, Drawer 닫기 목표 크기, Toast live-region과 모바일 폭을 표준화했다.
- 핵심 primitive axe 검사와 theme 소유권·중복 utility selector 테스트를 추가했다.
- 그록 리뷰를 반영해 외부 `aria-describedby` 병합, 탭 가로 44px, 상주 Toast live region, CSS source size 예산을 보강했다.
- Windows 200% reflow에서 발견된 게시판 가로 overflow를 막기 위해 Tabs flex item root에 `min-w-0` 계약을 추가했다.
- 195px 동등 확대 폭에서는 게시판 검색 행과 카드가 세로로 reflow하도록 해 입력·본문·CTA의 문서 overflow를 제거했다.
- 홈 캐러셀은 inline flex 최소폭과 offscreen slide paint를 컨테이너 안에 격리해 확대 시 문서 폭을 밀지 않도록 했다.
- 240px 이하에서는 게시판 글쓰기 FAB를 문서 흐름의 전체폭 버튼으로 전환해 탭·본문과의 겹침을 방지했다.

# #215 동문 수첩·행사·내 정보 핵심 여정 개선

- 동문 수첩의 검색 조건·결과·무결과·조회 실패를 구분하고 필터 초기화·재시도 행동을 추가했다.
- raw 공개 범위와 하이픈 fallback을 영향 중심 한국어와 공개 정보 부족 상태로 교체하고 상세 버튼·모달 닫기 영역을 44px로 맞췄다.
- RSVP 404만 정상 미신청으로 변환하고 참여 신청·대기·취소의 현재 의미와 다음 행동을 정리했다.
- 내 정보에 공개 범위 영향, 변경 여부, 저장 완료 상태, 최초 조회 실패 재시도와 사무국 문의를 추가했다.
- 일반 회원용 동문 수첩 API에 조회자별 공개 범위를 서버에서 강제하고, 학번·역할·계정 상태를 제외한 `DirectoryMemberRead` 계약을 추가했다. 관리자 상세는 권한이 적용된 전용 API로 분리했다.
- OpenAPI·TypeScript DTO를 재생성했으며 DB 스키마와 migration 변경은 없다.
- 그록 리뷰를 반영해 숨김 회원과 미존재 회원의 상세 404 code를 `member_not_found`로 통일하고, RSVP optional 조회도 `rsvp_not_found` code만 미신청으로 변환하도록 좁혔다.
- Windows visible E2E에서 발견된 이름·기수 변경 요청의 16px 터치 높이를 공통 Button·Field 44px 계약으로 교체하고 모바일 inline form reflow 테스트를 보강했다.

# #131 minimatch 개발 도구 GHSA 제거

- `eslint-plugin-import@2.32.0`의 허용 범위 `minimatch ^3.1.2` 안에서 lockfile을 `3.1.5`로 갱신했다.
- 강제 major override 없이 `GHSA-3ppc-4f35-3m26`, `GHSA-7r86-cg39-jmmj`, `GHSA-23c5-xmqv-rm74`를 제거했다.
- production dependency audit 0건을 유지하고 Web lint·test·production build로 개발 도구 호환성을 검증한다.

# #143 자동 E2E hard gate 복구

- 안정화 기간에 남아 있던 `web-e2e-cdp`의 `continue-on-error`를 제거해 실패가 PR을 차단하도록 복구했다.
- repo guard에 E2E soft-fail 재도입 금지 검사를 추가하고 CI 품질 게이트 문서를 실제 동작과 맞췄다.
- `true`뿐 아니라 표현식·별도 값으로 `continue-on-error` 키를 다시 선언하는 우회도 거부하도록 가드를 강화했다.
- `main-quality-gates` ruleset과 적용 스크립트의 required checks에 `e2e`를 추가해 체크 실패가 실제 병합을 차단하도록 연결했다.
- quoted YAML key를 포함한 soft-fail 변형과 ruleset의 `e2e` 누락을 pytest fixture로 고정해 가드 자체의 회귀도 검증한다.
- hard gate 첫 CI에서 드러난 Puppeteer/Next 병렬 실행 flake를 막기 위해 E2E spec 파일을 순차 실행하도록 고정했다.
