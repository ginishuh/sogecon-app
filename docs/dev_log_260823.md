# 2026-08-23

- Added: 가입 승인 안내 메일을 관리자 화면에서 공용 Gmail SMTP로 발송. 메일에는 활성화 링크만 넣고 토큰 원문을 별도 붙여넣지 않는다.
- Added: `POST /admin/signup-requests/{id}/send-activation-email`, `SMTP_*`/`PUBLIC_SITE_URL` 설정, 레이트리밋 `RATE_LIMIT_ACTIVATION_EMAIL`.
- Changed: 가입신청 심사 화면에 `안내 메일 보내기`를 추가하고 복사 UX는 실패 시 보조 경로로 유지.
- Changed: 가입 승인 안내 메일을 브랜드 HTML 카드로 꾸미고, 헤더 이미지 1장을 CID로 첨부한다.
- Changed: 홈·FAQ·개인정보 처리방침·문의·조직도 화면에 사무국 메일 `sogangecon.alumni@gmail.com`을 넣었다.
- Fixed: 홈 히어로가 `/media/images` 업로드 사진을 API 주소로 풀어 next/image 오류를 없앤다.
- Changed: 안내 메일 레이트리밋을 신청 ID가 아니라 엔드포인트 공용 한도로 묶고, 발송 이력을 남긴다.
- Changed: SMTP 전달 실패는 502, 로컬 `PUBLIC_SITE_URL` 기본값은 localhost, staging/prod는 빈 값·localhost·TLS 끄기를 거절한다.
