# Dev Log 2026-08-01

## D3 푸시 구독 보안·성능 (#248/#250/#264)

- Ownership (#248): `upsert_subscription`/`delete_subscription`가 actor `member_id`와 행 소유자를 대조. 타인 소유 → `403` `subscription_forbidden`. 없는 delete는 204. NULL `member_id` 레거시는 귀속 허용. upsert는 `ON CONFLICT (endpoint_hash)`로 동시 재동기화 원자화.
- Crypto (#250): `PUSH_ENCRYPT_AT_REST=true` 시 Settings 기동에서 `PUSH_KEK` base64·길이(16/24/32) fail-closed. `decrypt_str`는 `enc:v1:` 복호화 실패 시 `CryptoError`. stats `encryption_enabled`는 effective 설정.
- Perf (#264): stats는 `count_active_subscriptions` + `aggregate_since`. `send_to_all`/예약 발송은 `create_logs_batch` + `remove_by_endpoint_hashes`(100개 단위).
- Review follow-up: 발송 경로에서 `CryptoError`를 구독별 실패로 격리(평문 전송 금지). 예약 발송 예외는 `failed` 마감 + failed 행 재시도 회수. 관리자 `send_to_all`은 손상 구독 하나로 전체 중단하지 않음.
- Tests: `tests/api/test_d3_push_authority.py`, `test_crypto_utils.py` TEST DRIFT(암호문 그대로 반환) 제거.
- Docs: `docs/pwa_push.md` 소유권·fail-closed·배치 계약 반영.

## PR #284 후속 — 예약 발송 복구 경계와 KEK 검증

- 예약 로그에 `updated_at`과 구독별 `scheduled_notification_deliveries`를 추가해 `pending/in_progress` stale claim을 회수하고, 외부 Push 전에 claim을 커밋하도록 했다. 완료된 구독은 재시도에서 건너뛰며, 프로세스 중단으로 결과가 불확실한 claim은 `unknown`으로 보존해 중복 재전송을 막는다.
- 예약 발송의 DB/provider 예외는 세션 rollback 후 `failed`로 마감하고, 예외 처리 중에도 delivery 상태를 보존한다. 배치 로그·delivery 결과는 구독별이 아니라 배치 단위 commit을 유지한다.
- `PUSH_KEK` 설정과 공통 디코더 모두 `base64.b64decode(validate=True)`를 사용해 유효 문자 외 입력을 fail-closed로 거부한다.
- 회귀 테스트는 stale 회수, 부분 발송 후 재시도 중복 방지, DB 예외 뒤 failed readback, strict base64 검증을 포함한다.

## PR #285 리뷰 후속 — 예약 delivery 복구 경계 보완

- claim 범위를 실제 외부 Push 1회로 줄이고 endpoint별 결과를 즉시 commit해, 배치 중단 시 아직 보내지 않은 뒤쪽 endpoint가 `unknown`으로 함께 탈락하지 않게 했다.
- provider의 `(False, None)`처럼 결과가 불확실한 응답은 재시도하지 않고 `unknown`으로 고정한다. 외부 호출 전 복호화 검증 실패만 재시도 가능한 `failed`로 남긴다.
- due 이벤트 처리와 독립적인 5분 stale sweep을 scheduler에 추가하고, 최종 예약 로그 commit 실패도 rollback 후 `failed` finalize 경계에서 처리한다.
- 현재 대상에서 빠진 pending/failed delivery는 `abandoned`로 정리해 완료 집계에 남지 않도록 했다.

## D4 게시글 API/Web 계약 (#251/#258/#262/#263)

- Visibility (#251): `apps/api/post_visibility.py` SSOT. 공개 `GET /posts/`·비관리자 상세에 board vs notice/news 가시성 적용. 비공개 상세는 `404 post_not_found`. Hero는 동일 SSOT 사용.
- view_count (#258): `PostCreate` 입력에서 제거, create 시 0 강제. OpenAPI/DTO 재생성.
- Search (#262): 공개 `GET /posts/?q=` + board 서버 검색(debounce). 클라 페이지 필터 제거.
- RQ (#263): `lib/query-keys.ts`의 `postKeys`/`adminPostKeys`. create/update/delete prefix invalidate.
- Tests: `tests/api/test_d4_posts_authority.py`, news/notice TEST DRIFT(발행 후 공개 list), board.test 서버 `q` 검증.

## PR #286 리뷰 후속 — draft preview·검색 UX·카테고리

- API host-only 세션 쿠키를 운영 웹 호스트로 넓히지 않고, 관리자 draft를 `/admin/posts/{id}/preview` 브라우저 경로에서 조회하도록 분리했다. 공용 `apiFetch`에서는 `next/headers` 서버 의존성과 전체 쿠키 전달을 제거하고, 공개 `/posts/{id}` SSR은 익명 공개 경로로 유지한다.
- board 검색 `maxLength={100}` + `q` 슬라이스, `keepPreviousData`로 검색 전환 스켈레톤 깜빡임 완화.
- create/update category를 6개 Literal 계약으로 고정하고, 회원 생성은 board 4개로 제한해 notice/news가 201 후 영구 비공개가 되지 않게 했다. 생성 category 누락·미지 값과 update 명시적 null은 스키마 422다.
- board 게시글 수정은 notice/news 전용 폼 값으로 category를 덮어쓰지 않고 기존 category와 공개 상태를 보존한다.
- LIKE 이스케이프 회귀: `100%_exact`는 포함, `1000Zexact`는 제외.

## PR #286 리뷰 후속 2 — 권한별 preview·예약 링크·운영 audit

- 공개 `GET /posts/{id}`는 공개 가시성만 담당하고, 관리자의 draft/future 조회는
  `GET /admin/posts/{id}/preview`로 분리했다. 이 endpoint는 `admin_posts` 또는
  `admin_hero`를 허용하며, `admin_hero`는 Web에서 본문만 읽고 수정·삭제 액션을 보지 않는다.
- `/admin/posts`, `/admin/hero`, 홈 hero의 미발행·예약 게시글 링크는
  `/admin/posts/{id}/preview`로 연결한다. 공개 상세 링크는 `published_at <= now`일 때만
  `/posts/{id}`를 사용한다.
- board 게시글의 실제 category 변경 요청은 `board_category_immutable` 422로 거절해,
  성공처럼 보이는 무시 동작을 제거했다. update category의 unset 런타임 값이 `None`인
  Pydantic/OpenAPI 트릭은 스키마 주석으로 명시했다.
- D4 배포 전 audit은 `docs/agent_runbook_vps.md`의 read-only SQL을 실행한다. 결과가
  없으면 진행하고, `unpublished_count > 0` 또는 NULL/미지 category 행이 있으면 자동
  backfill하지 않고 별도 데이터 정리·판단으로 이관한다. 운영 DB는 이번 작업에서 조회·변경하지 않았다.

## PR #286 브라우저 E2E 검증

- 실제 production Web + deterministic mock API를 기동하고 Puppeteer 24.43.1과
  `/usr/bin/google-chrome`으로 CDP E2E를 실행했다. 초기 실행은 Web을 API base 없이
  빌드한 뒤 `127.0.0.1:3001`로 연결해 production CSP가 API 요청을 차단하는 runner
  환경 불일치로 실패했다.
- CI와 동일하게 `NEXT_PUBLIC_WEB_API_BASE=http://127.0.0.1:3001`을 build/start
  양쪽에 적용해 재실행한 결과 4 specs / 5 tests가 모두 통과했다. 관리자 hero 초안의
  preview 진입·본문 표시·편집 링크 차단, 익명 공개 상세 404, 기존 home/onboarding/
  directory E2E를 실제 브라우저에서 확인했다.
- Cursor IDE Browser의 visible browser에서도 같은 로컬 환경을 확인했다. `admin_hero`
  세션으로 홈의 `E2E 관리자 초안`·`관리자 미리보기`, `/admin/posts/42/preview`의
  제목·본문·편집/삭제 액션 부재를 확인했고, `member` 세션의 `/posts/42`는 API 404와
  `소식을 찾지 못했습니다.` 화면을 보였다.
- visible browser에서는 배너가 뷰포트 밖이라 클릭 대신 href 검증 후 동일 URL 직접 이동으로
  preview 도달을 확인했다. 따라서 route/link contract는 PASS이며 클릭 제스처 자체는
  headless E2E의 실제 `page.click` 결과로 보완한다. 공개 경계도 익명 로그아웃이 아니라
  비관리자 `member` 세션 검증임을 구분한다.
- `172f179` pull 후 Cursor IDE Browser visible rerun에서 위 제한을 해소했다. `admin_hero`
  세션으로 배너를 뷰포트 중앙으로 스크롤한 뒤 실제 CTA click으로
  `/admin/posts/42/preview`에 도달했고 제목·본문 표시와 edit/삭제 액션 부재를 확인했다.
  `anonymous` 세션에서는 `/auth/session` 401, `/posts/42` 404, 익명 헤더의 `로그인`과
  `소식을 찾지 못했습니다.` 화면을 확인해 관리자 preview 및 공개 draft 차단을 최종 PASS로
  확정했다.

## Playwright Agent CLI 로컬 브라우저 E2E 표준화

- `@playwright/cli@latest`를 전역 설치하고 `playwright-cli install --skills=agents`로
  저장소 `.agents/skills/playwright-cli`를 초기화했다. Chrome을 기본 브라우저로 발견했고,
  `AGENTS.md`에 로컬 production Web + mock API를 대상으로 한 Playwright CLI visible
  browser 우선 원칙을 추가했다.
- `pnpm -C apps/web e2e`의 Puppeteer 자동 회귀와 Playwright CLI visible browser 증거를
  별도 결과로 보고하고, named session·실제 click·anonymous 401/404·직접 이동 대체 금지
  규칙을 저장소 지침으로 고정했다.
- `playwright-cli` named session으로 현재 로컬 3000/3001을 직접 검증했다. `admin_hero`
  세션에서 snapshot ref를 사용한 실제 `click`으로 preview에 진입해 본문 표시와 edit/
  삭제 액션 0건을 확인했고, 별도 `anonymous` session에서 `/auth/session` 401,
  `/posts/42` 404, `로그인` 및 `소식을 찾지 못했습니다.` 화면을 확인했다. WSL에는 X
  서버가 없어 `--headed`는 실행하지 못했으며, headless Chrome CLI 결과로 구분해 기록한다.

## PR #286 리뷰 후속 3 — 관리자 수정 진입점·hero 권한·조회수

- 최신 P1의 관리자 draft/예약 게시글 수정 404를 해결했다. `/admin/posts/{id}/edit`는
  공개 `GET /posts/{id}` 대신 권한 있는 `GET /admin/posts/{id}/preview`를 사용하므로,
  관리자 목록 → 수정 화면에서도 본문을 읽고 저장할 수 있다. 공개 상세의 draft/future
  404 경계와 admin preview 권한은 그대로 유지한다.
- 최신 P1의 권한 경계 불일치를 해결했다. 일반 `admin` 등급만 있는 세션은
  `admin_posts`/`admin_hero`를 자동 상속하지 않으며, API의 `include_unpublished=true`와
  Web hero의 관리자 preview 링크 모두 두 세부 권한 또는 `super_admin`일 때만 활성화된다.
  세부 권한 없는 관리자는 draft hero 메타데이터를 받지 못하고 preview도 403이다.
- 연결된 P2도 함께 복구했다. 공개 `GET /posts/{id}`에서 관리자 상세 확인은
  `view_count`를 증가시키지 않고, 익명/일반 공개 조회만 증가시킨다. 관리자 편집은
  preview API를 사용해 이 통계를 건드리지 않는다.
- 회귀 검증을 추가했다: API generic admin hero 차단·관리자 조회수 제외, Web 관리자
  edit preview API 호출·generic admin hero 경계, production Web + deterministic mock
  API CDP E2E의 관리자 목록 → draft 수정 실제 클릭을 포함했다. 최종 전체 E2E는
  4 specs / 6 tests PASS, Web unit은 72 files / 253 tests PASS, API는 279 passed다.
- 최신 production build는 `NEXT_PUBLIC_WEB_API_BASE=http://127.0.0.1:3001`을 build/start
  양쪽에 주입해 확인했다. Playwright Agent CLI headless Chrome에서도 관리자 목록의
  `수정` 실제 click 후 `/admin/posts/42/edit` 제목·본문, hero CTA 실제 click 후
  `/admin/posts/42/preview` 본문·액션 0건, anonymous `/posts/42`의 HTTP 404와
  `소식을 찾지 못했습니다.`를 확인했다. WSL의 X 서버 부재로 headed 모드는 별도
  미검증으로 남긴다.

## PR #286 WSLg headed 브라우저 검증 정정

- 위 기록의 “WSL에 X 서버가 없어 headed 미검증”은 환경 진단 오류였다. WSLg와 X11/
  Wayland 소켓은 정상이며, 당시 Codex 셸 프로세스에 `DISPLAY`와 `WAYLAND_DISPLAY`가
  전달되지 않았던 것이 원인이었다. `DISPLAY=:0`, `WAYLAND_DISPLAY=wayland-0`,
  `XDG_RUNTIME_DIR=/run/user/1000`, `PULSE_SERVER=unix:/mnt/wslg/PulseServer`를
  현재 실행에 주입해 `xdpyinfo` X11 11.0 연결을 확인했다.
- 같은 환경에서 `playwright-cli --headed --browser=chrome`를 실제 기동하고,
  `admin_hero` 홈 hero CTA를 실제 click해 `/admin/posts/42/preview`로 이동한 뒤
  `E2E 관리자 preview 본문`을 확인했다. 따라서 headed 브라우저도 PASS이며 WSLg
  장애가 아니다. GPU 가속은 `llvmpipe` 소프트웨어 렌더링이지만 이번 UI 계약 검증의
  결과에는 영향을 주지 않는다.

## PR #286 리뷰 후속 4 — 게시글 쓰기 권한·관리자 상세 집계 경계

- 최신 리뷰의 P1을 반영해 게시글 mutation 권한을 `admin_posts`로 정렬했다. 일반 회원은
  기존처럼 board 4종을 작성할 수 있지만, `admin_hero` 또는 generic `admin`만 가진
  세션은 `POST/PATCH/DELETE /posts`를 403으로 거부한다. Web의 공개 상세 수정·삭제
  액션도 `admin_posts`가 없는 제한 관리자에게 렌더링하지 않는다.
- 관리자 게시물 목록의 제목 링크는 발행 상태와 무관하게
  `/admin/posts/{id}/preview`로 연결한다. 따라서 서버 컴포넌트의 공개 SSR에서 세션
  쿠키가 사라져 관리자 조회가 익명 조회수로 합산되는 경로를 제거하고, preview 화면의
  관리자 액션은 `admin_posts`가 있을 때만 제공한다. preview에서 삭제하면 관리자
  목록으로 돌아간다.
- 공개성 경계를 넘는 category 변경도 차단했다. board → notice/news뿐 아니라
  notice/news 초안을 board로 바꿔 발행 시각 없이 즉시 공개하는 역방향 요청도
  `board_category_immutable` 422로 거부한다. board 집합은 Literal 선언 순서가 아니라
  명시적 4개 집합으로 고정했고, 배포 audit에는 예약 발행 건수와 정상 판정 기준을
  추가했다.

## PR #286 리뷰 후속 5 — 제한 관리자 게시판 작성·상태 필터·액션 경계

- `admin_posts`가 없는 `admin`/`admin_hero` 세션은 관리자 공지·소식 관리 권한을
  얻지 않지만, 회원 작성 경로로 board 4종을 작성할 수 있게 했다. 이 fallback은
  `author_id`, `pinned`, `published_at`을 서버에서 다시 결정하며 notice/news는
  `invalid_post_category` 422로 거부한다.
- board 상세의 수정·삭제 액션과 `/board/{id}/edit` 권한 판정을 generic `admin`
  등급이 아니라 `admin_posts` 세부 권한에 맞췄다. 제한 관리자가 실패할 API 액션을
  화면에서 보지 않도록 하고, board category와 공개 상태 보존 계약은 유지한다.
- 관리자 게시물 목록의 `published_at` 상태를 공개·예약·초안으로 분리했다. API의
  `status=published|scheduled|draft` 필터와 Web 상태 배지를 동일하게 `now` 기준으로
  맞춰 미래 게시물이 발행됨으로 표시되거나 공개 필터에 섞이지 않게 했다.
- Playwright CLI가 저장하는 root storage state 패턴을 `.gitignore`에 추가했다. 인증
  쿠키가 들어갈 수 있는 `auth*.json`, `storage-state-*.json`, `*-auth-state.json`은
  저장소에 추적되지 않는다.
