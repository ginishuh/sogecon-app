name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  repo-guards:
    if: ${{ github.event.pull_request.draft == false }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12.3'
      - uses: actions/setup-node@v4
        with:
          node-version: '24.12.0'
      - name: Resolve PNPM version
        id: pnpm-version
        run: echo "version=$(scripts/resolve_pnpm_version.sh)" >> $GITHUB_OUTPUT
      - name: Pin pnpm version via Corepack
        run: |
          corepack enable
          corepack prepare pnpm@${{ steps.pnpm-version.outputs.version }} --activate
          pnpm -v
      - name: Install workspace tooling (commitlint)
        run: pnpm install --frozen-lockfile
      - name: Install repository guard dependencies
        run: python -m pip install --user ruff PyYAML==6.0.3
      - name: Run repository guards
        run: python ops/ci/guards.py
      - name: Git hooks integration tests
        run: bash ops/ci/test_githooks.sh
      - name: Enforce standardized versions
        run: python ops/ci/check_versions.py
      - name: Validate Dependabot policy
        run: python ops/ci/check_dependabot.py
      - name: Lint commit messages (PR commits)
        run: |
          pnpm exec commitlint \
            --config docs/commitlint.config.cjs \
            --from "${{ github.event.pull_request.base.sha }}" \
            --to "${{ github.event.pull_request.head.sha }}"

  python:
    needs: repo-guards
    if: ${{ github.event.pull_request.draft == false }}
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: appdb_test
          POSTGRES_USER: app
          POSTGRES_PASSWORD: devpass
        ports:
          - "5434:5432"
        options: >-
          --health-cmd="pg_isready -U app -d appdb_test"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: apps/api/requirements*.txt
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r apps/api/requirements.txt -r apps/api/requirements-dev.txt
          pip install pip-audit bandit
      - name: Lint
        run: ruff check apps/api
      - name: Type check
        run: python -m pyright --project pyrightconfig.json
      - name: Pytest collect guard
        env:
          GITHUB_BASE_REF: ${{ github.base_ref }}
          TEST_DB: pg
          TEST_DB_URL: postgresql+psycopg://app:devpass@localhost:5434/appdb_test
        run: python ops/ci/pytest_guard.py
      - name: Run tests
        env:
          TEST_DB: pg
          TEST_DB_URL: postgresql+psycopg://app:devpass@localhost:5434/appdb_test
        run: |
          echo "[ci] wait for Postgres (localhost:5434/appdb_test)"
          for i in {1..30}; do
            (echo > /dev/tcp/127.0.0.1/5434) >/dev/null 2>&1 && break || true
            sleep 1
          done
          pytest -q
      - name: Security scan (bandit)
        run: bandit -r apps/api -x apps/api/migrations -q
      - name: Dependency audit (pip-audit)
        run: |
          pip-audit -r apps/api/requirements.txt -r apps/api/requirements-dev.txt \
            --strict
      - name: Compile bytecode
        run: python -m compileall apps/api

  contract:
    needs: repo-guards
    if: ${{ github.event.pull_request.draft == false }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: apps/api/requirements*.txt
      - name: Install API dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r apps/api/requirements.txt -r apps/api/requirements-dev.txt
      - name: Resolve PNPM version
        id: pnpm-version
        run: echo "version=$(scripts/resolve_pnpm_version.sh)" >> $GITHUB_OUTPUT
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ steps.pnpm-version.outputs.version }}
      - uses: actions/setup-node@v4
        with:
          node-version: '24.12.0'
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml
      - name: Install schema tool dependencies
        run: pnpm -C packages/schemas install --frozen-lockfile
      - name: Verify OpenAPI/DTO sync
        run: |
          python scripts/export_openapi.py
          pnpm -C packages/schemas run gen-dts
          git diff --exit-code packages/schemas/openapi.json packages/schemas/index.d.ts

  web:
    needs: repo-guards
    if: ${{ github.event.pull_request.draft == false }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24.12.0'
      - name: Resolve PNPM version
        id: pnpm-version
        run: echo "version=$(scripts/resolve_pnpm_version.sh)" >> $GITHUB_OUTPUT
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ steps.pnpm-version.outputs.version }}
      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_OUTPUT
        id: pnpm-cache
      - uses: actions/cache@v6
        name: Cache pnpm store
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - name: Install dependencies
        run: pnpm -C apps/web install --frozen-lockfile
      - name: Lint
        run: pnpm -C apps/web lint
      - name: Unit tests
        run: pnpm -C apps/web test
      - name: Cache Next.js build
        uses: actions/cache@v6
        with:
          path: apps/web/.next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('pnpm-lock.yaml', 'apps/web/next.config.js', 'apps/web/package.json') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-
      - name: Build
        run: pnpm -C apps/web build
      - name: Accessibility smoke (axe)
        run: pnpm -C apps/web exec vitest run __tests__/a11y-smoke.test.tsx --silent
      - name: Bundle size check (raw JS total ≤ 1536KB)
        run: node apps/web/scripts/check-bundle-size.mjs --max-kb 1536 --exclude-prefix app/admin/
      - name: Dependency audit (pnpm, prod only)
        run: |
          pnpm -C apps/web audit --audit-level=high --prod

  secrets-scan:
    if: ${{ github.event.pull_request.draft == false }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ github.token }}

  semgrep:
    if: ${{ github.event.pull_request.draft == false }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: returntocorp/semgrep-action@v1
        with:
          config: p/ci
