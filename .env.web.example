# Web(Next.js)용 환경변수 예시(.env.web.example)
# - 이 값들은 "빌드타임"에 고정됩니다. 컨테이너 재시작만으로는 바뀌지 않습니다.
# - 실제 사용 시 이 파일을 복사해 `.env.web` 로 두고, 빌드 스크립트에서 필요 값은
#   커맨드 환경변수로 직접 주입하는 것을 권장합니다.

# 1) 필수 — 도메인/엔드포인트
# 공개 사이트 URL (예: https://segecon.wastelite.kr)
NEXT_PUBLIC_SITE_URL=https://segecon.wastelite.kr
# API 베이스 URL (예: https://api.segecon.wastelite.kr)
NEXT_PUBLIC_WEB_API_BASE=https://api.segecon.wastelite.kr

# 2) 웹 푸시(선택)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=

# 3) 분석/관측(선택)
NEXT_PUBLIC_ANALYTICS_ID=
# 릴리스 커밋(선택; RUM 이벤트 태깅)
NEXT_PUBLIC_COMMIT_SHA=

# 4) 서비스워커/보안(선택)
# 프로덕션 기본: SW 사용, CSP는 엄격. 프리뷰/개발에서만 완화하세요.
NEXT_PUBLIC_ENABLE_SW=1
# 프리뷰에서 DevTools/HMR 등을 위해 CSP 완화가 필요할 때만 1
NEXT_PUBLIC_RELAX_CSP=

# 5) 이미지 외부 도메인(쉼표 구분). 예: img.cdn.example.com,static.example.com
NEXT_PUBLIC_IMAGE_DOMAINS=

